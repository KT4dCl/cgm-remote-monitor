app = "ktlcgm"

[build]
  dockerfile = "Dockerfile.flyio"

[env]
  PORT = "8080"

[[services]]
  internal_port = 8080
  protocol = "TCP"
  [services.port.80]
    handlers = ["http"]
  [services.port.443]
    handlers = ["tls", "http"]
